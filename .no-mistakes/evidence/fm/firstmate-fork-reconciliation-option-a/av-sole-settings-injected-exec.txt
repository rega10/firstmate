### Case A: worker template carries '--settings <json>' (space form) + --dangerously-skip-permissions
--settings
{"apiKeyHelper":null,"feedbackDrafts":"off","env":{"ANTHROPIC_API_KEY":null,"ANTHROPIC_AUTH_TOKEN":null}}
--permission-mode
bypassPermissions
--allowedTools
Bash
--model
sonnet
-p
do the task
--settings occurrences: 1

### Case B: worker template carries inline '--settings=<json>' form
--settings
{"apiKeyHelper":null,"feedbackDrafts":"off","env":{"ANTHROPIC_API_KEY":null,"ANTHROPIC_AUTH_TOKEN":null}}
--permission-mode
bypassPermissions
--allowedTools
Bash
--model
opus
-p
do the task
--settings (space form) occurrences: 1
inline --settings= occurrences: 0

Both cases: exactly one authoritative --settings (the AV credential-scrubbing JSON) reaches the injected claude; the worker template's own --settings is stripped.
